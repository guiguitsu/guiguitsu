export component DetailPanel inherits Rectangle {
    background: #2a3d3d;

    Text {
        text: "Detail Panel";
        color: #c0c8d0;
        font-size: 18px;
        horizontal-alignment: center;
        vertical-alignment: center;
    }
}
